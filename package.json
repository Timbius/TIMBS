{
  "name": "timbs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "npm --prefix backend run start",
    "dev:api": "npm --prefix backend run start",
    "dev:web": "npm run dev"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
